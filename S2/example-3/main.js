const age = 15;
const isOverAge = age >= 18 ? true : false;

const word = 'Strawberry';
const isLongWord = word.length > 8 ? 'is a big word' : 'is short word'; 
const isLongWord2 = word.length > 8 ? (word.length > 12 ? 'is a huge word' : 'is a big word') : 'is short word'; 



